name = "brahma-browser"
compatibility_date = "2024-01-01"

# Cloudflare Pages configuration
pages_build_output_dir = "."

# No workers needed - static site only
# No D1, R2, or KV needed - pure static HTML
